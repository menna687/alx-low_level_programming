#include <stdio.h>

/**
 * main - print digits from 1 to 9
 * Return: 0 in case of success (no errors)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");

	return (0);
}
